/**
 *
 * create by ligx
 *
 * @flow
 */
import go, { goBack, goForward, replace, } from './go';
import Link from './Link';
import { createApp, createRoute, render, Redirect, } from './router';

export {
  go,
  goBack,
  goForward,
  replace,
  Link,
  createApp,
  createRoute,
  render,
  Redirect
};
